server:
  port: 8080
  address: 0.0.0.0

spring:
  datasource:
    url: ${DATABASE_URL}
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      # ddl-auto: update

jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000
